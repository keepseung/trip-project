spring:
  datasource:
    url: jdbc:h2:mem:triple_db
    username: sa
    password:
    driver-class-name: org.h2.Driver
    hikari:
      jdbc-url: jdbc:h2:mem:triple_db
  jpa.properties.hibernate.dialect: org.hibernate.dialect.H2Dialect

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type: trace